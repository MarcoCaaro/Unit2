package ejercicioswhile;

import java.util.Scanner;

public class Ejercicio4 {
public static void main(String[] args) {
	
	//Inicializamos el Scanner
	Scanner sc = new Scanner(System.in);
	
	//Declaramos la variable entera del contador
	int contador=0;
	
	//Declaramos la variable entera de la suma de positivos
	int sumaPositivos=0;
	
	//Declaramos la variable entera de la suma de negativos
	int sumaNegativos=0;
	
	//Declaramos la variable entera del contador de numeros negativos
	int contNegativos=0;
	
	//Declaramos la variable entera del numero
	int numero;
	
	
	while (contador<10) {
		numero = sc.nextInt();
	if (numero > 0)
		
	}
}
}
